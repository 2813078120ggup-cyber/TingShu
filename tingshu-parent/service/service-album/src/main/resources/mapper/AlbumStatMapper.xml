<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.atguigu.tingshu.album.mapper.AlbumStatMapper">
    <select id="getAlbumInfoStat" resultType="map">
        select
            max(if(stat.stat_type = '0401', stat_num, 0)) as playStatNum,
               max(if(stat.stat_type = '0402', stat_num, 0)) as subscribeStatNum,
               max(if(stat.stat_type = '0403', stat_num, 0)) as buyStatNum,
               max(if(stat.stat_type = '0404', stat_num, 0)) as  commentStatNum
        from album_stat stat where stat.album_id=#{albumId}
    </select>
</mapper>

